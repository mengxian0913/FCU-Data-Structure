#include<stdio.h>
#include<stdlib.h>

typedef struct {
  int val;
  Node *next;
}Node;

int main() {
  // Node *head = (Node*)malloc(sizeof(Node));
  // int n;
  // scanf("%d", &n);
  //
  // Node *current = head;
  // 
  // for(int i = 0; i < n; i++) {
  //   current -> val = i + 1;
  //   current = (Node *)malloc(sizeof(Node));
  //   current = current -> next;
  // }
  //
  // while(head != NULL) {
  //   printf("Val: %d, Pointer: %p\n", head -> val, header -> next);
  //   head = head -> next;
  // }

  return 0;
}
